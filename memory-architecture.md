# Memory Architecture

*Stub — full document coming soon.*

This document will cover the evolution of the memory format used across Jade Path agents (plain prose summaries → JSON key-value pairs → Markdown), the reasoning behind each transition, and the tradeoffs that led to the current approach.